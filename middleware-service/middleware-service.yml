server:
  port: 9100

mail:
  otp:
    template: 2-09i93837ooP
  reset:
    password:
      template: x-0292929299929992

springdoc:
  server-url: ${SWAGGER_DOC_URL}
  swagger-ui:
    tagsSorter: alpha
  writer-with-order-by-keys: true


spring:
  profiles:
    include: users
  codec:
    max-in-memory-size: 7048576
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${ISSUER_HOST}/.well-known/authorization-server/jwks.json

